import { IArgumentInput } from '../../components/command/argument/IArgumentInput';

export const args: IArgumentInput[] = [
    {
        arg: '[id]',
        description: 'The id of the project.'
    }
];
